[tool.ruff]
line-length = 100
select = ["E", "F", "S"]  # S = bandit-style security rules

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]

[tool.bandit]
exclude_dirs = [".venv", "tests"]
skips = ["B101"]

[tool.pytest.ini_options]
addopts = "-q --strict-markers"

# PLAN — 할 일 CLI

- [x] `todo.py add <text>` 로 할 일을 db.json 에 추가
- [x] `todo.py list` 로 할 일 목록 출력 (id + text + done 여부)
- [x] `todo.py done <id>` 로 완료 표시
- [x] `todo.py rm <id>` 로 삭제
- [x] pytest 로 각 명령에 대한 단위 테스트
- [x] README 작성 (설치, 예시, 전체 동작 GIF 없이 텍스트로 시연)
"""
dutch_pay.py — 친구들과 식당에서 더치페이할 때 쓰는 계산기.

Claude가 docs/SPEC.md를 읽고 이 파일을 구현합니다.

진행자 백업: docs/flawed_reference.py를 이 파일로 복사하면
Claude 없이도 시연 가능합니다.
"""

# Claude가 구현할 함수들:
#   split_evenly(amount, people)
#   add_tax(amount, tax_rate=0.1)
#   add_tip(amount, tip_rate=0.05)
#   calculate_total(menu_total, tax_rate=0.1, tip_rate=0.05)
#   split_with_exclusions(amount, people, exclusions=None)
#   format_won(amount)
